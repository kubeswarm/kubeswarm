version: "2"
run:
  allow-parallel-runners: true
linters:
  default: none
  enable:
    - copyloopvar
    - dupl
    - errcheck
    - ginkgolinter
    - goconst
    - gocyclo
    - govet
    - ineffassign
    - lll
    - modernize
    - misspell
    - nakedret
    - prealloc
    - revive
    - staticcheck
    - unconvert
    - unparam
    - unused
    - gosec
  settings:
    gosec:
      excludes:
        # G104: Errors unhandled - already enforced by errcheck
        - G104
        # G115: Numeric conversion overflow - too noisy for controller code
        - G115
    revive:
      rules:
        - name: comment-spacings
        - name: import-shadowing
    modernize:
      disable:
        - omitzero
  exclusions:
    generated: lax
    rules:
      - linters:
          - lll
        path: api/*
      - linters:
          - dupl
          - lll
        path: internal/*
      - linters:
          - dupl
          - lll
        path: pkg/*
      # cmd/main.go is an orchestration entrypoint; complexity limit not meaningful here.
      - linters:
          - gocyclo
        path: cmd/main.go
      # test/utils launches kubectl subprocesses - G204 is expected and intentional.
      - linters:
          - gosec
        path: test/
    paths:
      - third_party$
      - builtin$
      - examples$
formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
