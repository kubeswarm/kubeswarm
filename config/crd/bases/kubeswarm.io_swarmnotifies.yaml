---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: swarmnotifies.kubeswarm.io
spec:
  group: kubeswarm.io
  names:
    categories:
    - kubeswarm
    kind: SwarmNotify
    listKind: SwarmNotifyList
    plural: swarmnotifies
    shortNames:
    - swnfy
    - swnfys
    singular: swarmnotify
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.channelCount
      name: Channels
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SwarmNotify defines a reusable notification policy that routes run events to
          one or more channels (Slack, generic webhook, etc.).
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SwarmNotifySpec defines the desired state of SwarmNotify.
            properties:
              channels:
                description: Channels lists the notification targets.
                items:
                  description: NotifyChannelSpec defines a single notification channel.
                  properties:
                    slack:
                      description: Slack configures a Slack incoming webhook channel.
                        Required when type is "slack".
                      properties:
                        webhookURLFrom:
                          description: WebhookURLFrom reads the Slack incoming webhook
                            URL from a Secret key.
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      required:
                      - webhookURLFrom
                      type: object
                    template:
                      description: |-
                        Template is an optional Go template for the message body.
                        Context is NotifyPayload. Overrides the channel's default format.
                        For "webhook": template output is the raw POST body.
                        For "slack": template output replaces the default Block Kit message text.
                      type: string
                    type:
                      allOf:
                      - enum:
                        - webhook
                        - slack
                      - enum:
                        - webhook
                        - slack
                      description: Type determines the channel implementation.
                      type: string
                    webhook:
                      description: Webhook configures a generic HTTP POST channel.
                        Required when type is "webhook".
                      properties:
                        headers:
                          description: Headers are additional HTTP headers included
                            in every request.
                          items:
                            description: WebhookHeader defines a single HTTP header
                              added to outbound webhook requests.
                            properties:
                              name:
                                description: Name is the HTTP header name.
                                type: string
                              value:
                                description: Value is the literal header value.
                                type: string
                              valueFrom:
                                description: ValueFrom reads the header value from
                                  a Secret key.
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            required:
                            - name
                            type: object
                          type: array
                        method:
                          default: POST
                          description: Method is the HTTP method. Defaults to POST.
                          enum:
                          - GET
                          - POST
                          - PUT
                          - PATCH
                          type: string
                        url:
                          description: URL is the webhook endpoint as a literal string.
                          type: string
                        urlFrom:
                          description: URLFrom reads the URL from a Secret key. Takes
                            precedence over URL.
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - type
                  type: object
                  x-kubernetes-validations:
                  - message: webhook config can only be set when type is webhook
                    rule: self.type == 'webhook' || !has(self.webhook)
                  - message: slack config can only be set when type is slack
                    rule: self.type == 'slack' || !has(self.slack)
                minItems: 1
                type: array
              "on":
                description: |-
                  On lists the events that trigger notifications.
                  If empty, all events fire.
                items:
                  description: NotifyEvent is the type of event that triggers a notification.
                  enum:
                  - TeamSucceeded
                  - TeamFailed
                  - TeamTimedOut
                  - BudgetWarning
                  - BudgetExceeded
                  - DailyLimitReached
                  - AgentDegraded
                  type: string
                type: array
              rateLimitSeconds:
                default: 300
                description: |-
                  RateLimitSeconds is the minimum interval between notifications for the
                  same (team, event) pair. Default: 300. Set to 0 to disable rate limiting.
                minimum: 0
                type: integer
            required:
            - channels
            type: object
          status:
            description: SwarmNotifyStatus defines the observed state of SwarmNotify.
            properties:
              channelCount:
                description: ChannelCount is the number of configured notification
                  channels.
                type: integer
              conditions:
                description: Conditions reflect the current state of the SwarmNotify.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastDispatches:
                description: LastDispatches records the most recent dispatch result
                  per channel index.
                items:
                  description: NotifyDispatchResult records the most recent dispatch
                    attempt for one channel.
                  properties:
                    channelIndex:
                      description: ChannelIndex is the zero-based index of the channel
                        in spec.channels.
                      type: integer
                    error:
                      description: Error is the last error message when Succeeded
                        is false.
                      type: string
                    lastEvent:
                      description: LastEvent is the event type that triggered the
                        most recent dispatch.
                      enum:
                      - TeamSucceeded
                      - TeamFailed
                      - TeamTimedOut
                      - BudgetWarning
                      - BudgetExceeded
                      - DailyLimitReached
                      - AgentDegraded
                      type: string
                    lastFiredAt:
                      description: LastFiredAt is when the most recent dispatch attempt
                        was made.
                      format: date-time
                      type: string
                    succeeded:
                      description: Succeeded is false when all retry attempts failed.
                      type: boolean
                  required:
                  - channelIndex
                  - succeeded
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the .metadata.generation this status
                  reflects.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
